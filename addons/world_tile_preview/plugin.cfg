[plugin]

name="World Tile Preview"
description="Editor-only: renders world TileMapLayer nodes from CSV files so map authoring (NPC/marker placement) works visually. Preview tiles are added as internal child nodes and never saved to the .tscn — the runtime MapLoader still owns the actual tile load."
author="Adventure Land"
version="1.0.0"
script="plugin.gd"
